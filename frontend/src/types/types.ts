export type Plant = {
    name: string;
    image_url: string;
    scientific_name:string;
    id: number;
}

export type User = {
    id: string;
    email: string;
    username: string;
    password: string;
}